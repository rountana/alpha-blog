<% if obj.errors.any? %>
<!-- display the class name in error message -->
    <h2>Following errors prevented <%= obj.class.name.downcase %> from being saved </h2>
    <ul>
    <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
    </ul>
<% end %>
